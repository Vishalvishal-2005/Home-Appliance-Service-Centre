import React from 'react';
/**
 * A functional component that renders a greeting message.
 *
 * This component displays a simple "Hello" message within an HTML
 * heading element. It is typically used as part of a user interface
 * to welcome users or indicate successful login.
 *
 * @returns {JSX.Element} A React element containing the greeting message.
 *
 * @example
 * // Usage of the Login component
 * import Login from './Login';
 *
 * function App() {
 *   return (
 *     <div>
 *       <Login />
 *     </div>
 *   );
 * }
 */
const Login =() => {
  return (
    <div>
        <h1>Hello</h1>
        <h1>Hello</h1>
    </div>
  );
};
export default Login;