import React from 'react';
/**
 * A functional component that renders a greeting message.
 *
 * This component displays two headings with the text "Hello".
 *
 * @returns {JSX.Element} A React element representing the greeting message.
 *
 * @example
 * // Usage of the Login component
 * import Login from './Login';
 *
 * function App() {
 *   return (
 *     <div>
 *       <Login />
 *     </div>
 *   );
 * }
 */
const Login =() => {
  return (
    <div>
        <h1>Hello</h1>
        <h1>Hello</h1>
    </div>
  );
};
export default Login;